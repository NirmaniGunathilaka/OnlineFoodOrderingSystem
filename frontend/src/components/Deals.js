import React from "react";
import { Box, Typography } from "@mui/material";

export default function Deals() {
    return (
        <Box mt={5}>
            <Typography variant="h5">Deals Here!</Typography>
        </Box>
    );
}