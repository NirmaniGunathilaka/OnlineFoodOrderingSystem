import React from "react";
import { Box, Typography } from "@mui/material";

export default function Cart() {
    return(
        <Box mt={5}>
            <Typography variant="h5">Cart Details Here!</Typography>
        </Box>
    );
}